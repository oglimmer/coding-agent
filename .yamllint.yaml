extends: default

rules:
  line-length: disable
  document-start: disable
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1

ignore: |
  helm/coding-agent/templates/
  frontend/node_modules/
